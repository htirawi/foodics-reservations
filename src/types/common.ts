/**
 * Common utility types used across the application.
 */

/**
 * Translation function signature.
 * Takes an i18n key and returns the translated string.
 */
export type TranslateFn = (key: string) => string;
