# E2E Test Environment Variables
# This file is LOCAL ONLY and should NEVER be committed
# Used by Playwright tests to ensure no real tokens leak

# Test-only token (not a real Foodics token)
VITE_FOODICS_TOKEN="e2e-test-token"

# API base URL (not used in offline mode, but required for typing)
VITE_API_BASE_URL="http://localhost:5173/api"

# Optional: Online mode flags (off by default)
# Set PW_E2E_ONLINE=true to run contract tests against staging
# PW_E2E_ONLINE=false
# PW_API_BASE_URL=https://api.foodics.dev/v5
# PW_STAGING_TOKEN=your-staging-token-here
